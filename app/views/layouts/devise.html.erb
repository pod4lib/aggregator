<% content_for(:content) do %>
  <%= content_for(:header) %>

  <div class="container">
    <div class="col-md-8 offset-md-2 devise-wrap">
      <%= yield %>
    </div>
  </div>
<% end %>

<%= render template: 'layouts/application' %>
